model:
  name: "XGBoostRegressor"
  version: "1.0.0"
  n_estimators: 100
  max_depth: 6
  learning_rate: 0.05
  subsample: 0.8

paths:
  model_output: "models/yield/model.joblib"
  metrics_output: "models/yield/metrics.json"
