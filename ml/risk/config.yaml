model:
  name: "XGBoostClassifier"
  version: "1.0.0"
  n_estimators: 100
  max_depth: 5
  learning_rate: 0.05

paths:
  model_output: "models/risk/model.joblib"
  metrics_output: "models/risk/metrics.json"
